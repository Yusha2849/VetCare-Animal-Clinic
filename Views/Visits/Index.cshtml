@model IEnumerable<VetCare_Animal_Clinic.Models.Visit>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Appointment)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.VisitComments)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Appointment.Appointment_Reason)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.VisitComments)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VisitID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VisitID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VisitID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
